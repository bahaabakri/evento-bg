export enum Role {
    USER = 'user', // Regular user (coming from website)
    ADMIN = 'admin', // Admin user (coming from admin panel)
    MODERATOR = 'moderator', // Moderator user (coming from admin panel)
    SUPER_ADMIN = 'super_admin' // Super admin user (coming from admin panel)
}